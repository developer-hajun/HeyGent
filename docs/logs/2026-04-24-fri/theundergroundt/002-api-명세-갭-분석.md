# 작업 로그

## 날짜

2026-04-24

## 작성자

theundergroundt

## 관련 브랜치 / PR

- 브랜치: AI-feat/semantic-step-재사용-규칙
- PR: 없음

## 작업 목적

- `tmp/docs/WEB to BACKEND 기준 API ...csv` 명세를 기준으로 현재 `S14P31E105` 프로젝트의 구현 범위와 차이를 확인한다.
- 이후 웹 백엔드 API 개발 시 우선 구현해야 할 영역과 재사용 가능한 AI 백본 기능을 구분한다.

## 변경 요약

- CSV 명세 60개 엔드포인트를 카테고리별로 확인했다.
- 현재 저장소의 실제 FastAPI 라우트는 `AI/app/api` 아래의 health, tasks, providers, websocket 중심임을 확인했다.
- 명세의 제품 백엔드 URL과 현재 AI 백본 URL은 대부분 직접 일치하지 않으며, 일부 기능만 개념적으로 재사용 가능하다고 판단했다.

## 주요 파일

- `AI/app/api/router.py`
- `AI/app/api/http/tasks.py`
- `AI/app/api/http/providers.py`
- `AI/app/api/http/health.py`
- `AI/app/api/ws/gateway.py`
- `tmp/docs/WEB to BACKEND 기준 API 34c8bd1e73fd8042a0d8f2f581fb8b78.csv`

## 테스트 / 확인

- 파일 읽기와 라우트 검색으로 확인했다.
- 테스트 실행은 하지 않았다.
- `git` 기본 명령은 safe.directory 문제로 실패했고, 브랜치 확인은 비영구 `-c safe.directory=...` 옵션으로 수행했다.

## 결정 / 이슈

- CSV는 인증, 세션, 루틴, 에이전트, 시각화, 메모리, 외부서비스, AI모델, 헬스데이터, 알람, 디바이스, 설정까지 포함하는 제품 백엔드 계약이다.
- 현재 `AI` 프로젝트는 FastAPI 기반 AI 백본 프로토타입이며, 웹 백엔드 계약의 일부 하위 기능만 제공한다.
- 개발 시에는 CSV URL을 외부 계약으로 두고, 현재 AI 백본 API를 그대로 노출하기보다 어댑터 또는 별도 백엔드 계층에서 매핑하는 방식이 필요하다.

## 다음 단계

- 우선순위는 인증/사용자 컨텍스트 확정, WebSocket 경로 정합, TaskRun 시각화 API 매핑, 세션/메시지 API 도입 순서가 적절하다.
- `taskRuns` 명세와 현재 `tasks` 라우트의 모델 차이를 별도 설계로 고정해야 한다.
