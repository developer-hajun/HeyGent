# 작업 로그

## 날짜

2026-05-10

## 작성자

전희수

## 관련 브랜치 / PR

- 브랜치: AI-feat/subagent-fe-connection
- PR: 미정

## 작업 목적

- 작업 모드 입력을 작업 단위로 저장하고 실행 이력과 연결할 수 있는 백엔드 기반을 추가합니다.

## 변경 요약

- 작업 도메인 모델, 정책, 서비스, 저장소 계약을 추가했습니다.
- Postgres 작업 보드 테이블과 마이그레이션을 추가했습니다.
- 세션 작업 목록, 생성, 상태 변경, 댓글, 실행 이력, 컨텍스트 미리보기 HTTP API를 추가했습니다.
- 작업 생성 직후 실행 시작 실패 시 작업은 유지하고 상태를 `todo`로 되돌리며 시스템 댓글을 남기도록 처리했습니다.

## 주요 파일

- `AI/app/domain/work/*`
- `AI/app/contracts/work/*`
- `AI/app/api/http/work.py`
- `AI/app/storage/postgres/work_repository.py`
- `AI/app/storage/postgres/schema.py`
- `AI/app/storage/postgres/migrations.py`
- `AI/tests/domain/test_work_service.py`
- `AI/tests/storage/test_postgres_durable_contracts.py`

## 테스트 / 확인

- `python -m compileall app`
- `python -m pytest tests\domain\test_work_service.py tests\storage\test_postgres_durable_contracts.py`
- `python -m pytest tests\api\test_health.py tests\api\test_tasks_runtime.py`
- 새 작업 코드와 테스트에서 금지된 출처 표현이 남지 않았는지 검색했습니다.

## 결정 / 이슈

- 작업 모드 생성의 초기 상태는 `in_progress`로 시작합니다.
- 실행 시작 실패는 작업 생성 롤백이 아니라 `todo` 전환과 시스템 댓글로 기록합니다.
- 완료된 작업은 댓글 재개 요청으로 `todo`가 될 수 있지만, `cancelled` 작업은 먼저 복구해야 합니다.

## 다음 단계

- 프론트 작업 보드 구조를 `work` 폴더 기준으로 정리하고 작업 모드 입력 흐름을 연결합니다.
- 실제 화면에서 작업 생성, 보드 표시, 실행 이력 표시를 검증합니다.
