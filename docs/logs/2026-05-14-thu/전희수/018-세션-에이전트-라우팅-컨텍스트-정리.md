# 작업 로그

## 날짜

2026-05-14

## 작성자

전희수

## 관련 브랜치 / PR

- 브랜치: AI-feat/Agent-Skills-routin
- PR: 미정

## 작업 목적

- 세션 에이전트 라우팅 후보 프롬프트에서 스킬 사용 예시가 과하게 주입되는 문제를 줄인다.
- 기본 제공 에이전트의 할 수 있는 일 설명을 라우팅 기준으로 더 명확하게 만든다.

## 변경 요약

- 세션 에이전트 후보 payload에서 스킬 `When to use` 발췌 주입을 제거했다.
- 후보 프롬프트의 스킬 설명은 스킬명과 한 줄 description만 표시하도록 정리했다.
- 여러 세션 에이전트가 같은 스킬을 가질 때 description은 공용 스킬 설명 영역에 한 번만 표시하도록 정리했다.
- 세션 에이전트 후보 컨텍스트가 현재 실행 에이전트의 직접 skill 카탈로그보다 먼저 나오도록 순서를 조정했다.
- 직접 skill 카탈로그가 세션 후보보다 우선되는 정보가 아니라 현재 실행 에이전트의 직접 skill 목록임을 명시했다.
- 기본 제공 에이전트 description을 담당 업무 범위가 드러나도록 보강했다.
- 사용 예시가 후보 프롬프트에 포함되지 않는 회귀 테스트를 추가했다.

## 주요 파일

- `ai/app/api/session_agent_profiles.py`
- `ai/app/domain/orchestration/prompts/prompt_builder.py`
- `ai/app/domain/agents/templates.py`
- `ai/tests/api/test_session_agent_profiles.py`
- `ai/tests/test_model_loop_contract.py`

## 테스트 / 확인

- `python -m pytest ai\tests\test_model_loop_contract.py ai\tests\domain\test_agent_templates.py ai\tests\api\test_session_agent_profiles.py ai\tests\domain\test_capability_resolver.py -q`
- Docker AI 컨테이너 재빌드 후 새 세션에 기본 제공 에이전트를 생성하고, 강남역 지하철 지갑 분실 요청을 전송했다.
- 실테스트에서 `session_agent_task`가 호출되고 `requiredSkillNames=["subway-lost-property"]`로 K-에이전트 하위 실행이 완료되는 것을 확인했다.

## 결정 / 이슈

- 팀장 라우팅은 에이전트의 할 수 있는 일, 스킬 이름, 공용 스킬 description을 기준으로 하고, 구체적인 사용 예시는 실행 에이전트가 스킬 본문을 읽는 단계에서 확인한다.
- 현재 실행 에이전트의 직접 skill 목록은 세션 후보보다 더 직접적으로 맞는 경우에만 사용하고, 후보가 더 맞으면 세션 에이전트 배정을 검토한다.

## 다음 단계

- 필요하면 기본 제공 에이전트 설명 문구를 실제 테스트 입력 기준으로 더 다듬는다.
