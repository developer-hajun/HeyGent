# 로컬 전용 워크스페이스 규칙

## 적용 범위

- 이 문서는 `C:\Users\sangjikim\e105\agentcoding\S14P31E105`에 적용합니다.
- 현재 로컬 워크스페이스와 이 컴퓨터의 환경 구성을 위한 지침입니다.

## 목적

- 아키텍처 초안 작성과 코드 패치 추적을 위한 로컬 작업 규칙을 유지합니다.
- 저장소 루트와 `AI/`, `docs/`, `work_logs/` 같은 주요 작업 영역을 함께 다룹니다.

## 로컬 경로 기준

- 이 문서 안의 모든 로컬 경로 기준 루트는 `C:\Users\sangjikim\e105\agentcoding\S14P31E105`로 봅니다.
- `docs/...`, `AI/...` 같은 상대 경로는 모두 이 루트 기준으로 해석합니다.
- 로컬 작업을 문서화할 때는 다른 컴퓨터의 예전 경로보다 현재 저장소 구조를 우선 사용합니다.

## 코드 패치 초안 규칙

- 모든 코드 패치는 `docs/codedraft/YYYY-MM-DD/` 아래에 기록을 생성하거나 갱신해야 합니다.
- 파일명 규칙:
  - `1-codefetch.md`
  - `2-codefetch.md`
  - `3-codefetch.md`
- 각 `codefetch` 기록에는 아래 내용을 포함해야 합니다.
  - 변경된 파일 목록
  - 각 파일을 변경한 이유
  - 각 파일을 어떻게 변경했는지
  - 검증 또는 테스트 메모

## API 명세 규칙

- API가 추가되거나 변경되면 아래 경로의 최신 API 누적 문서를 갱신합니다.
  - `docs/YYYY-MM-DD/3. API-변경-누적-명세.md`
- 가능하면 기존 내용을 덮어쓰지 말고 새 항목을 이어서 추가합니다.
- 상태는 아래처럼 명확히 표시합니다.
  - `planned`
  - `implemented`
  - `revised`
  - `deprecated`

## 작업 방식

- orchestration, execution, provider 책임은 분리해서 유지합니다.
- 세부 동작을 확장하기 전에 구조 변경은 최소 단위로 점진적으로 진행합니다.
- 아키텍처 변경이 생기면 먼저 백본 의도를 문서화한 뒤 코드 패치를 진행합니다.

## 로컬 선호 규칙

- 이 문서는 현재 컴퓨터와 워크스페이스 구조를 위한 로컬 전용 지침입니다.
- 이 컴퓨터에서 저장소 루트가 바뀌면 이 문서도 함께 갱신합니다.
- 추적되는 프로젝트 문서로 의도적으로 옮기지 않았다면, 저장소 공용 규칙으로 취급하지 않습니다.
