# 협업 작업 규약

## 적용 범위

- 이 문서는 `S14P31E105` 루트와 그 하위 전체에 적용합니다.
- 더 하위 폴더에 별도 `AGENTS.md`가 있으면 해당 문서가 우선합니다.

## 목적

- 협업 과정에서 남겨야 하는 공용 문서 기준을 통일합니다.
- PR, 의미 있는 `push`, 설계 결정, 인수인계 내용을 `docs/...` 아래에서 일관되게 관리합니다.

## 문서화 원칙

- 협업 공유가 필요한 모든 의미 있는 작업 결과는 항상 `docs/...` 아래에 요약 기록합니다.
- 기본 기록 단위는 파일 저장이나 사소한 수정이 아니라, PR 생성 또는 공유 가치가 있는 `push`입니다.
- 리뷰 대응, 구현 방향 변경, 테스트 상태 변경처럼 다른 작업자가 알아야 할 변화가 있으면 문서를 추가하거나 갱신합니다.
- 로컬에서만 참고할 임시 메모는 `docs/...`가 아니라 로컬 전용 영역에서 관리합니다.
- 로그 템플릿과 세부 작성 규칙은 `docs/AGENTS.md`를 기준으로 통일합니다.

## docs 구조

- 작업 로그: `docs/logs/YYYY-MM-DD-day/<git-user.name>/001-작업-요약.md`
- 설계 결정: `docs/decisions/YYYY-MM-DD-주제-요약.md`
- 인수인계: `docs/handoffs/YYYY-MM-DD-작성자.md`

## 로그 작성 규칙

- 날짜 폴더는 `2026-04-24-thu` 형식을 사용합니다.
- 작성자 폴더는 기본적으로 `git config user.name` 값을 사용합니다.
- 로그 파일명은 시간 대신 인덱스를 사용합니다.
- 형식은 `001-작업-요약.md`, `002-작업-요약.md`처럼 작성합니다.
- 같은 날짜와 같은 작성자 폴더 안에서만 순번을 증가시킵니다.

## 로그 필수 항목

- 날짜
- 작성자
- 관련 브랜치 또는 PR
- 작업 목적
- 변경 요약
- 주요 파일
- 테스트 또는 확인 내용
- 결정, 이슈, 리스크
- 다음 단계

## 운영 원칙

- 여러 하위 프로젝트에 공통으로 영향이 있는 내용은 루트 `docs/...`에 기록합니다.
- 특정 하위 프로젝트 전용 규칙이나 템플릿이 필요하면 해당 하위 폴더에 별도 `AGENTS.md`를 둡니다.
- 코드 변경보다 설계 합의가 더 중요한 경우에는 먼저 설계 결정 문서를 남기고 구현을 진행합니다.

## 보안 규칙

- 비밀값, 토큰, 인증 정보, 민감한 로컬 경로는 문서에 적지 않습니다.
- 긴 터미널 원문이나 외부 문서 원문 전체를 복사하지 말고 결과만 요약합니다.
